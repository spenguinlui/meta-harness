你現在在 ai-infra-management 專案內。

**目標**：目前 EC2 月費 $697（cost-summary.json 的 ec2_instances），我想把它砍到剩一半以下（$300/mo 上下）。請給我具體可執行的方案。

要求：

1. 不要只給「升級型號」「換 Graviton」「Reserved Instance」這類純財務手段 — 這些頂多砍 20–30%，不夠。請從**架構層**找到能砍一半的方法。
2. 必須引用 `state/<project>/production/compute.json` 與 `monitoring.json` 的真實數據（哪幾台、CPU/memory 利用率多少）來支撐你的方案。
3. 說明：哪些專案受影響、做什麼、預估省多少、風險。

直接給結論，不要先問我問題。
