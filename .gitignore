# 本機 target repo 清單（路徑、domain 描述等）— 不上 git（錨定根目錄：fixtures/ 內同名測試資料要入版控）
/targets.yml

# 顧問動筆前的 audit trail（「打算改什麼／為什麼」）— 不上 git
prescriptions/*
!prescriptions/README.md

# 顧問對話歷史（含真實 PII / infra ID / 業主對話）— 不上 git
sessions/*
!sessions/README.md

# 任務性 case（特定 target 的蒸餾，他人 fork 不該看到別人任務）— 不上 git
cases/*
!cases/README.md

# 自家未消化失敗清單（各 fork 自有自的）— 不上 git（錨定根目錄：fixtures/ 內同名測試資料要入版控）
/BACKLOG.md

# experiments/ 自驗 loop 的 raw runs 含 target 真實 infra ID / 專案名 — 不上 git
# 結構檔（README / gold / prompts / run.sh / eval.sh）入版控，runs/ 只是本機跑出來的證據
experiments/*/runs/

# ScheduleWakeup runtime 狀態 — 不上 git
.claude/scheduled_tasks.lock

# Stop hook 的架構指紋（上次通過自驗時的狀態）— 本機 runtime，不上 git
/.claude/.state/

# /upkeep 每週維生產出的健康時序 log（含本機路徑 / 執行結果）— 本機 runtime，不上 git
/experiments/meta-harness-eval/upkeep-log.md

# 深驗（LLM-judge）滾動報告（含本機 prescription 分析）— 本機 runtime，不上 git
/experiments/meta-harness-eval/deep-verify-report.md
